{
  "name": "<%= _.slugify(appname) %>",
  "version": "0.0.0",
  "dependencies": {
    <% if(installBootstrap) { %>"bootstrap": "~3.0.0", <% } %>
    <% if(installModernizr) { %>"modernizr": "~2.6.2", <% } %>
    "jquery": "~1.10.2"
  },
  "devDependencies": {}
}